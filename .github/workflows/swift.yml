name: Swift Build & Test

on:
  push:
    branches: [main, master, develop]
  pull_request:

jobs:
  swift-version-check:
    runs-on: macos-15
    steps:
    - uses: actions/checkout@v4
    - name: Check Swift version
      run: swift --version

  lint:
    runs-on: macos-15
    steps:
    - uses: actions/checkout@v4
    - name: Install SwiftLint
      run: brew install swiftlint
    - name: Run SwiftLint
      run: swiftlint --strict Sources/ || true

  build-and-test:
    runs-on: macos-15
    strategy:
      matrix:
        xcode: ['16.2']
    steps:
    - uses: actions/checkout@v4
    - name: Select Xcode ${{ matrix.xcode }}
      run: sudo xcode-select -s /Applications/Xcode_${{ matrix.xcode }}.app || sudo xcode-select -s /Applications/Xcode.app
    - name: Build with Swift Package Manager
      run: swift build -v
    - name: Run Tests
      run: xcodebuild test -scheme Kumo -destination 'platform=macOS' -skipPackagePluginValidation
